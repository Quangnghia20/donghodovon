# donghodovon

git clone ...

Kiem tra trang thai:
```git status```


Add new code:
```git add .```

Tao diem nho <br>
```git commit -m"nghia first commit"```

Dua len Server
```git push```

lay tu server ve
```git pull```